#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    multiset<long long > s;
    long long n, k;
    cin >> n >> k;
    for(int i=0;i<n;i++)
    {
        long long x;
        cin >> x;
        s.insert(x);
    }
    



}