#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;
    while (t--) {
int n;
        cin >> n;
        int q;
        cin >> q;
multiset<pair <int,int> > s;
        for(int i = 0; i < n; i++)
        {
            long long x,y;
            cin >> x >> y;
            long long z=x*y;
           // s.insert(z,);
        }

    }

  
}